pipeline{
    	agent any
	environment{
			APP="Java"
			ENV="prod"
		}
    	stages{
		stage("check"){
			steps{
			sh 'env'
		}
	  }	   
	  	stage("BUILD"){
			steps{
			echo "in GROOVY----- APP_TYPE: $APP, ENV: $ENV "
			sh '''
				echo "in shell------ APP_TYPE: $APP, ENV: $ENV"
			'''
		}
	  }      
    }
}
